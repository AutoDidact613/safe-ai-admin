const ROLES = ["coordinator", "secretary", "teacher", "student"];

const ASSIGNMENT_TYPES = ["exercise", "quiz_solution", "project"];

const SUBMISSION_STATUSES = ["not_submitted", "submitted", "in_review", "reviewed"];

module.exports = { ROLES, ASSIGNMENT_TYPES, SUBMISSION_STATUSES };
